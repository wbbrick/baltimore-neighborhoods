/*
 * Main file for browserify to consume.  Starts backbone by instantiating AppView
 */
//let AppView = require('./views/app');

// ( function() {
// 	let appView = new AppView();
// 	appView.setElement( '#content' ).render();
// 	console.log( 'test' );
// } )();
